default:
  app:
    name: Library API
    version: 1.0.0

  broker:
    mqtt:
      url: mqtt://test.mosquitto.org
      topics: ["publish/book/transaction"]
      qos:
      protocol: mqtt
      retain:
      subscribe: true

development:

test:

staging:

production:
